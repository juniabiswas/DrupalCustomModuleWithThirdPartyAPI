<?php

/**
 * Implementing hook_theme().
 */
function transport_details_theme($existing, $type, $theme, $path)
{

    return array(
        'route_list' => array(
            'variables' => array('info' => array(), 'title' => '')
        ),

        'schedule' => array(
            'variables' => array('info' => array(), 'title' => '', 'page_attributes' => array())
        )
    );
}
